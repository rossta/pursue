<% if flash.any? %>
  <div id="flash">
    <% flash.each do |key, value| -%>
      <div id="flash_<%= key %>" class="alert-box <%= flash_class(key) %>"><%= value %></div>
    <% end -%>
  </div>
<% end %>
